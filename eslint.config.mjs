/**
 * Root ESLint config — imports shared config from packages/config/eslint.
 * All rules, boundaries, and agency standards are defined there.
 */
import { baseConfig } from "./packages/config/eslint/index.mjs";

export default baseConfig;
