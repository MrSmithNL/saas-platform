/**
 * Authentication module — Clerk integration with multi-tenant organisations.
 * Handles: sign-up, sign-in, session management, organisation switching.
 */

// Placeholder — implementation in Phase 1
export const AUTH_MODULE = "auth" as const;
