/**
 * @saas-platform/feature-flags
 *
 * Feature flag system — per-tenant feature control.
 * Starts as config-based (Phase 1), upgraded to full service (Phase 3).
 */

export const FEATURE_FLAGS_MODULE = "feature-flags" as const;
