/**
 * @saas-platform/notifications
 *
 * Notification engine — email (Resend), push, in-app notifications.
 * Layer 3 service. Implementation in Phase 3.
 */

export const NOTIFICATIONS_MODULE = "notifications" as const;
