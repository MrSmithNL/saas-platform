/**
 * Events this module subscribes to from other modules.
 * Register handlers here — they will be called by the event bus.
 */

export const subscriptions = {
  // Example:
  // "billing.subscription.changed": async (event) => { ... }
};
