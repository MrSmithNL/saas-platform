/**
 * Module API surface — commands (writes) and queries (reads).
 * This is how verticals and the agent runtime interact with this module.
 */

export { commands } from "./commands.js";
export { queries } from "./queries.js";
