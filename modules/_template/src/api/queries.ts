/**
 * Read operations for this module.
 * Queries are side-effect-free and cacheable.
 */

export const queries = {
  // Example:
  // async getEntity(id: string): Promise<TemplateEntity | null> { ... }
};
