/**
 * Write operations for this module.
 * Commands modify state and may emit events.
 */

export const commands = {
  // Example:
  // async createEntity(input: CreateEntityInput): Promise<TemplateEntity> { ... }
};
